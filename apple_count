//https://repl.it

// // Hello_world.c

// #include <stdio.h>

// int main() {
//     printf("hello, world!\n");
//     return 0;
// }

// #include <stdio.h>

// main()

// {
//     int apple_count; // 사과의 갯수, 정수형
//     int price; // 사과의 가격, 정수형
//     int unit_price; // 사과1개의 가격, 정수형

//     // 변수는 어떻게 사용할까?
//     // 1) 변수에 값을 할당
//     // 2) 변수의 값을 가져옴(읽어옴)
//     apple_count = 15;
//     unit_price = 1000;
//     price = apple_count * unit_price;
    
//     //화면에 출력할 때 printf 함수를 사용한다.
//     printf("사과의 가격은 %d 원 입니다.\n", price); // 포맷지시어
// }

#include <stdio.h>

main()
{
    // 사과의 단가와 개수를 입력받아 전체 사과의 가격을 계산하는 것
    // 대원칙! 생각한 후 코딩
    // 필요한 변수를 정함.
    // 변수를 선언한다.
    int unit_price, apple_count, price;

    // 1) 사과의 단가(unit_price)를 입력받는다.
    // ---> "사과의 단가를 입력하세요" 라고 출력
    printf("사과의 단가를 입력하세요");
    // ---> 사과의 단가를 입력받음
    scanf("%d", &unit_price);

    // 2) 사과의 갯수(apple_count)를 입력받는다.
    printf("사과의 갯수를 입력하세요");
    scanf("%d", &apple_count);

    // 3) 사과 가격(price)을 계산한다.
    price = apple_count * unit_price;

    // 4) 사과 가격을 화면에 출력한다.
    printf("전체 사과의 가격은 %d원 입니다.\n",price);
}

